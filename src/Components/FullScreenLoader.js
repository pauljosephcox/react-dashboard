import React from 'react';

const FullScreenLoader = (props) => {
    return(
        <div className={'appstack loading'} {...props}>
            <div className="app-body"><h1>FullScreenLoader.js</h1></div>
        </div>
    )
};

export default FullScreenLoader;
